# Ralph Progress Log
Started: Thu Feb 19 17:08:21 CET 2026

## Codebase Patterns
- Lockfile module lives in `packages/core/src/lockfile/` — no barrel file existed before; created `index.ts` to re-export from `manager.ts` and `cleanup.ts`
- Core public API is at `packages/core/src/index.ts` — all new exports from core must be added here
- Tests use real temp directories (`mkdtemp`) with `beforeEach`/`afterEach` cleanup — no fs mocking
- Biome enforces single-line formatting for short function signatures and ternaries — don't multi-line them
- Import `.js` extensions required in all source files (ESM convention)
- `placeFile()` returns absolute paths — always normalize with `relative(projectRoot, result.path)` before storing as lockfile keys

---

## 2026-02-19T17:10 - US-001
- Implemented `removePlacedFiles(filePaths, projectRoot)` in `packages/core/src/lockfile/cleanup.ts`
- Uses `stat()` to distinguish files from directories, fixing the EISDIR bug where `unlink()` fails on directories
- After each removal, walks up parent directories and removes empty ones up to (not including) projectRoot
- ENOENT errors silently skipped for already-deleted paths
- Created barrel file `packages/core/src/lockfile/index.ts` re-exporting from `manager.ts` and `cleanup.ts`
- Added `removePlacedFiles` export to `packages/core/src/index.ts`
- Files changed:
  - `packages/core/src/lockfile/cleanup.ts` (new)
  - `packages/core/src/lockfile/cleanup.test.ts` (new — 10 tests)
  - `packages/core/src/lockfile/index.ts` (new — barrel)
  - `packages/core/src/index.ts` (added export)
  - `scripts/ralph/prd.json` (US-001 passes: true)
- **Learnings for future iterations:**
  - The existing `cleanupOrphanedFiles()` in `sync.ts` (lines 270-296) only uses `unlink()` which fails on directories — that's the bug
  - `core/src/index.ts` imports lockfile functions directly from `./lockfile/manager.js` — the new barrel doesn't change this to avoid churn; new exports use `./lockfile/cleanup.js`
  - Run `bun run lint` to check Biome formatting — it's strict about line length and ternary formatting
---

## 2026-02-19T17:14 - US-002
- Normalized lockfile integrity keys to use relative paths in `packages/cli/src/commands/sync.ts`
- Two locations where `placeFile()` returns absolute `result.path` are now converted to relative using `isAbsolute()` check + `relative(projectRoot, result.path)`
  - Content accumulator flush (memory+rules) — around line 1039
  - Command placement — around line 1100
- Other path sources (skills, files, IDE configs) already stored relative paths — no changes needed
- `cleanupOrphanedFiles()` resolver already handles both absolute and relative via `startsWith("/")` check — backward compat preserved
- Added `isAbsolute` and `relative` to the `node:path` import (line 2)
- Files changed:
  - `packages/cli/src/commands/sync.ts` (modified — 2 path normalization sites)
  - `scripts/ralph/prd.json` (US-002 passes: true)
- **Learnings for future iterations:**
  - `placeFile()` in `packages/core/src/placement/engine.ts` always returns absolute paths via `resolveAbsolutePath()` — any code storing `result.path` as a lockfile key must normalize to relative
  - Five path-storage sites in sync.ts: skills (line 906, already relative), content accumulator (line 1039, now fixed), commands (line 1100, now fixed), files (line 1153, already relative), IDE (line 1209, already relative)
  - The `relative()` function from `node:path` was already imported in the original code but not used — the import line just needed `isAbsolute` added
---

## 2026-02-19T17:17 - US-003
- Refactored `cleanupOrphanedFiles()` in `packages/cli/src/commands/sync.ts` to use `removePlacedFiles()` from `@baton-dx/core`
- Replaced inline `unlink()` + `rmdir()` + parent-directory-cleanup loop (old lines 270-296) with a single `removePlacedFiles(orphanedPaths, projectRoot)` call
- Removed unused imports: `unlink` and `rmdir` from `node:fs/promises`
- Added `removePlacedFiles` to the `@baton-dx/core` import block
- Orphan detection logic (Set difference), user prompt flow, spinner, and count reporting all unchanged
- Files changed:
  - `packages/cli/src/commands/sync.ts` (modified — replaced inline deletion with shared function)
  - `scripts/ralph/prd.json` (US-003 passes: true)
- **Learnings for future iterations:**
  - When removing inline code that used fs imports, always check if those imports are still needed elsewhere in the file before removing them — `readdir` and `stat` were still used, `unlink` and `rmdir` were not
  - The refactored cleanup function went from 30 lines to 3 lines — the shared `removePlacedFiles()` handles path resolution, stat-based file/dir detection, ENOENT skipping, and empty parent cleanup
  - Must run `bun run build` before `bun run typecheck` — the CLI resolves `@baton-dx/core` from its built `dist/` output
---
