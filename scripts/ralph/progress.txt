# Ralph Progress Log
Started: Sun Feb 22 19:14:06 CET 2026
---

## Codebase Patterns
- ClonedSource interface in git-clone.ts is the canonical return type for all git clone operations; any new fields must also be added to mock objects in github-resolver.test.ts and edge-cases.test.ts
- resolveGitHubSource returns its own ResolvedGitHubSource interface (subset of ClonedSource) — test assertions should not expect fields that aren't in the resolver's return type
- simpleGit mocking pattern: `vi.mock("simple-git")` at module level, then `vi.mocked(simpleGitModule.simpleGit).mockReturnValue({...})` in beforeEach
- Must run `bun run build` before `bun run typecheck` since CLI resolves @baton-dx/core from dist/
- Biome enforces parameter-per-line formatting for long function signatures
- Tests are co-located: `foo.test.ts` next to `foo.ts`

---

## 2026-02-22T19:18 - US-001
- What was implemented: Expanded ClonedSource interface with `cachePath: string` and `sparseCheckout: boolean` fields. Updated both return paths in cloneGitSource() (cache-hit and fresh-clone). Added `expandSparseCheckout()` function using `git sparse-checkout add`. Exported from core/src/index.ts.
- Files changed:
  - packages/core/src/sources/git-clone.ts (interface expansion + new function)
  - packages/core/src/index.ts (added expandSparseCheckout export)
  - packages/core/src/sources/git-clone.test.ts (added unit tests with mocked simpleGit)
  - packages/core/src/sources/github-resolver.test.ts (updated mock objects for new ClonedSource fields, fixed assertions)
- **Learnings for future iterations:**
  - When expanding ClonedSource, you MUST also update all mock objects in github-resolver.test.ts (7 occurrences) — TypeScript strict mode catches this via typecheck
  - The resolveGitHubSource function intentionally filters ClonedSource fields down to its own ResolvedGitHubSource type — test assertions should match the resolver's return type, not the full ClonedSource
  - Biome will reject long function signatures on one line — use parameter-per-line format
  - The existing git-clone.test.ts has integration tests (describe.skip) — add unit tests with vi.mock("simple-git") as separate describe blocks
---
