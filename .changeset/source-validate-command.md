---
"@baton-dx/cli": minor
---

Add `baton source validate` command that validates source repository structure and manifests with 15 checks covering schema validation, file references, variable consistency, and orphaned profile detection
