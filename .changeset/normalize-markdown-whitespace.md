---
"@baton-dx/cli": patch
---

Normalize markdown whitespace in merged content — collapse 3+ consecutive newlines to exactly 2 and ensure trailing newline in append, prepend, import strategies and content accumulator output.
