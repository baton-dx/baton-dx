name: minimal-starter
version: 1.0.0
description: A minimal Baton profile for getting started

ai:
  tools:
    - claude-code
    - cursor

  skills:
    - name: hello-world
      scope: project

  memory:
    - source: MEMORY.md
      merge: replace
